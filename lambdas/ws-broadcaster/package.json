{
  "name": "@msa2/ws-broadcaster",
  "version": "0.1.0",
  "private": true,
  "main": "dist/local.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/local.js",
    "start:local": "node dist/local.js",
    "dev": "ts-node-dev --respawn --transpile-only src/local.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no unit tests yet\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.633.0",
    "@aws-sdk/client-dynamodb": "^3.633.0",
    "@aws-sdk/lib-dynamodb": "^3.633.0",
    "@fastify/websocket": "^10.0.1",
    "fastify": "^4.28.1",
    "ioredis": "^5.4.1",
    "pino": "^9.3.2",
    "pino-pretty": "^11.2.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.141",
    "@types/node": "^20.14.10",
    "@types/ws": "^8.5.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
