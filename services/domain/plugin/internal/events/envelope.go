package events

import "github.com/teamboard/services/domain/plugin/internal/domain"

// Envelope is an alias so the rest of the events package uses a consistent name.
type Envelope = domain.Envelope
